<h1> Hello lets check if this is working </h1>

<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <%= simple_form_for [@car, @rental] do |f| %>
        <%= f.input :date %>
        <%= f.submit "Book", class: "btn btn-primary" %>
      <% end %>
   </div>
  </div>
</div>
